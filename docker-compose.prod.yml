version: "3.9"

services:
  backend:
    build:
      context: ./psych_backend
      dockerfile: Dockerfile
    command: >
      uvicorn app.main:app 
      --host 0.0.0.0 
      --port 8000 
      --workers 4

    environment:
      ENVIRONMENT: production
      DEBUG: "false"
    restart: always
